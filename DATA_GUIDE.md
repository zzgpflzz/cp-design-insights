# 데이터 관리 가이드

## 현재 데이터 구조

### 1. Firebase Firestore (실시간 데이터)
프로젝트의 메타데이터와 콘텐츠를 저장합니다.

**컬렉션:**
- `projects` - 월별 프로젝트
- `agendas` - 월별 주요 아젠다
- `roadmapProjects` - 로드맵 프로젝트
- `projectProgresses` - 프로젝트 진행상황
- `uiuxUpdates` - UI/UX 업데이트
- `tfTasks` - TF 업무

### 2. Google Analytics 데이터 (정적 CSV)
페이지별 세션 수 및 방문 통계를 제공합니다.

**파일:** `/public/analytics-data.csv`

**컬럼:**
- 방문 페이지 경로
- 유입 채널
- 세션
- 세션당 페이지 뷰
- 평균 세션 지속 시간

---

## 분석 데이터 업데이트 방법

### 현재 문제점
- CSV 파일이 정적이어서 수동으로 업데이트 필요
- 실시간 데이터 반영 불가
- Google Analytics 데이터를 수작업으로 다운로드 필요

### 제안하는 해결 방법

#### 옵션 1: Google Sheets 공개 링크 사용 (추천)
**장점:**
- Google Sheets에서 데이터 관리
- 실시간 업데이트 가능
- CSV 수동 업로드 불필요

**구현 방법:**
1. Google Sheets에 분석 데이터 입력
2. 시트를 CSV로 공개: `파일 > 공유 > 웹에 게시 > CSV`
3. 공개 URL을 코드에 설정
4. 앱이 자동으로 최신 데이터 가져오기

#### 옵션 2: Google Sheets API 사용
**장점:**
- 더 세밀한 제어 가능
- 비공개 시트 사용 가능

**단점:**
- API 키 설정 필요
- 비용 발생 가능 (무료 할당량 초과 시)

#### 옵션 3: 정기적 CSV 업데이트 (현재 방식)
**장점:**
- 간단하고 직관적
- 추가 설정 불필요

**단점:**
- 수동 작업 필요
- 실시간성 부족

---

## 추천 워크플로우

### 1단계: Google Sheets로 마이그레이션
```
현재 CSV → Google Sheets 업로드
→ 시트를 CSV로 웹에 게시
→ 코드에서 공개 URL 사용
```

### 2단계: 데이터 자동 동기화
- Google Sheets에서 데이터 수정
- 앱이 자동으로 최신 데이터 가져오기 (캐싱 옵션 포함)

### 3단계: 매칭 규칙 정리
프로젝트 링크와 분석 데이터 경로를 정확히 매칭:
- Firestore 프로젝트의 `link` 필드
- CSV의 `방문 페이지 경로` 컬럼
- 두 값이 정확히 일치해야 세션 수 표시

---

## 데이터 매칭 문제 해결

### 문제: 세션 수가 0 또는 적게 표시
**원인:**
1. 프로젝트 링크와 CSV 경로 불일치
2. CSV에 해당 경로 데이터 없음
3. 외부 URL 사용 (매칭 불가)

**해결:**
1. 프로젝트 링크 형식 통일
   - 내부 링크: `/blogs/event/...`
   - 외부 링크: 전체 URL 사용
2. CSV 경로와 정확히 일치하도록 설정
3. 매칭 로직 개선 (부분 매칭 지원)

---

## 다음 단계

### 즉시 할 수 있는 것:
- [ ] Google Sheets 생성 및 CSV 데이터 업로드
- [ ] 시트를 웹에 CSV로 게시
- [ ] 공개 URL 확인

### 구현이 필요한 것:
- [ ] `lib/analyticsData.ts` 수정 (Google Sheets URL 사용)
- [ ] 캐싱 전략 개선 (주기적 갱신)
- [ ] 매칭 로직 개선 (유연한 경로 매칭)

### 선택 사항:
- [ ] Google Sheets API 연동
- [ ] 실시간 동기화 구현
- [ ] 관리자 페이지에 통계 대시보드 추가

---

## 논의 히스토리

### 2026-07-01: 세션 수 표시 문제 발견
**문제:**
- 프로젝트 상세 페이지에서 세션 수가 너무 적게 표시됨
- 일부 프로젝트는 세션 수가 0으로 나옴

**원인 분석:**
1. CSV 파일 경로: `/blogs/event/nct_w_house2605` 형식
2. 프로젝트 링크가 이와 다른 형식이면 매칭 실패
3. 외부 URL 사용 시 경로 추출 후 매칭 안 됨
4. CSV 데이터가 오래되었을 가능성

**현재 상태:**
- CSV 파일: 1,111개 라인
- 대표 데이터 예시:
  - `/blogs/event/nct_w_house2605` → 14,435 세션 (twitter)
  - `/blogs/lfs/truz_fruitsgame` → 13,490 세션 (direct)
  - `/blogs/lfs/wishnini-house-puzzle` → 9,560 세션 (direct)

**이전 논의 내용:**
- Google 링크를 통한 데이터 연동 방법을 논의했었음 (구체적 방법 재확인 필요)

### 2026-07-07: 데이터 관리 체계화 논의
**결정 사항:**
- 데이터 관리 가이드 문서 생성 (DATA_GUIDE.md)
- 다른 우선순위 작업이 있어 데이터 관리 구현은 보류
- 향후 논의 재개 예정

**보류된 결정 사항:**
1. **Google Sheets 사용 의향은?**
   - 옵션 1 (공개 링크) vs 옵션 2 (API) vs 옵션 3 (현재 방식)

2. **데이터 업데이트 주기는?**
   - 실시간 필요? or 하루 1회? or 주 1회?

3. **프로젝트 링크 형식 통일은?**
   - 모든 프로젝트를 내부 링크로? or 외부 링크 허용?

4. **추가로 관리하고 싶은 데이터는?**
   - 클릭률(CTR)?
   - 전환율?
   - 기타 메트릭?

---

## 참고 문서
- [Google Sheets 웹 게시 가이드](https://support.google.com/docs/answer/183965)
- [Google Sheets API 문서](https://developers.google.com/sheets/api)
- Firebase Firestore 콘솔: https://console.firebase.google.com
